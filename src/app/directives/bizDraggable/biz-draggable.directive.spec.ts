import { BizDraggableDirective } from './biz-draggable.directive';

describe('BizDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new BizDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
