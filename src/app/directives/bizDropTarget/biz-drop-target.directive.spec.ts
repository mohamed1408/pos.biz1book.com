import { BizDropTargetDirective } from './biz-drop-target.directive';

describe('BizDropTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new BizDropTargetDirective();
    expect(directive).toBeTruthy();
  });
});
